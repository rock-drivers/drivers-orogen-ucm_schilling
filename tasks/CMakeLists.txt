# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(ucm_schillingTaskLib)
ADD_LIBRARY(${UCM_SCHILLING_TASKLIB_NAME} SHARED 
    ${UCM_SCHILLING_TASKLIB_SOURCES})

add_dependencies(${UCM_SCHILLING_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${UCM_SCHILLING_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${UCM_SCHILLING_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${UCM_SCHILLING_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${UCM_SCHILLING_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${UCM_SCHILLING_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${UCM_SCHILLING_TASKLIB_HEADERS}
    DESTINATION include/orocos/ucm_schilling)

